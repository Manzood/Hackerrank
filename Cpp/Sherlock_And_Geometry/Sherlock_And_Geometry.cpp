#include "bits/stdc++.h"
using namespace std;
#define debug(x) cout << #x << " = " << x << endl;
#define int long long

int32_t main () {
    int t;
    cin >> t;
    while (t--) {
        int xc, yc, R;
        scanf("%lld%lld%lld", &xc, &yc, &R);

        vector <vector <int>> T(3, vector <int> (2));
        for (int i = 0; i < 3; i++) {
            scanf("%lld%lld", &T[i][0], &T[i][1]);
        }

    }
}
